@model Affiliate.Utility.ViewModel.DashboardVM

<div class="d-flex justify-content-between align-items-center flex-wrap grid-margin">
    <div>
        <h4 class="mb-3 mb-md-0">Personal Information</h4>
    </div>
    <div class="d-flex align-items-center flex-wrap text-nowrap">
        <a class="mb-3 mb-md-0" style="font-size: 1.25rem; margin-top: 0; font-weight: 500; line-height: 1.2; display: block; margin-block-start: 1.33em; margin-block-end: 1.33em; margin-inline-start: 0px; margin-inline-end: 0px;color:black;" href="@Model.referralLink">Referral Code : @Model.rCode  <i style="margin-left:10px" data-feather="copy"></i></a>
        @*<div class="input-group flatpickr wd-200 me-2 mb-2 mb-md-0" id="dashboardDate">
                <span class="input-group-text input-group-addon bg-transparent border-primary" data-toggle><i data-feather="calendar" class="text-primary"></i></span>
                <input type="text" class="form-control bg-transparent border-primary" placeholder="Select date" data-input>
            </div>*@
        @*<button type="button" class="btn btn-outline-primary btn-icon-text me-2 mb-2 mb-md-0">
                <i class="btn-icon-prepend" data-feather="printer"></i>
                Print
            </button>
            <button type="button" class="btn btn-primary btn-icon-text mb-2 mb-md-0">
                <i class="btn-icon-prepend" data-feather="download-cloud"></i>
                Download Report
            </button>*@
    </div>
</div>

@*<div class="d-flex justify-content-between align-items-center flex-wrap grid-margin">
        <div>
            <h4 class="mb-3 mb-md-0">Personal Information</h4>
        </div>
        <div class="d-flex align-items-center flex-wrap text-nowrap">
            <h4 class="mb-3 mb-md-0">Referral Code : @Model.rCode</h4><a style="margin-left:10px" title="Copy link" href="@Model.referralLink"><i data-feather="copy"></i></a>
        </div>
    </div>*@
<div class="row">
    <div class="col-lg-8 col-xl-9 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                        </thead>
                        <tbody>
                            <tr>
                                <td style="width:10%">Full Name : </td>
                                <td style="width:40%" class="text-muted">@Model.user.LastName @Model.user.FirstName @Model.user.MiddleName</td>
                                <td style="width:10%">Bank : </td>
                                <td style="width:40%" class="text-muted">@Model.account.Bank.Name</td>
                                
                            </tr>
                            <tr>
                                <td style="width:10%">Email : </td>
                                <td style="width:40%" class="text-muted">@Model.user.Email</td>
                                <td style="width:10%">Account Name : </td>
                                <td style="width:40%" class="text-muted">@Model.account.AccountName</td>
                                
                            </tr>
                            <tr>
                                <td style="width:10%">Role : </td>
                                <td style="width:40%" class="text-muted">@Model.user.Role.Name</td>
                                <td style="width:10%">Account Number : </td>
                                <td style="width:40%" class="text-muted">@Model.account.AccountNumber</td>
                            </tr>
                        </tbody>
                    </table>
                </div>
                @*<div class="d-flex flex-column">
                    <div class="row">
                        <div class="col-lg-6 col-xl-6">
                            <label class="tx-11 fw-bolder mb-0 text-uppercase">Full Name:</label>
                            <label class="text-muted">@Model.user.LastName @Model.user.FirstName @Model.user.MiddleName</label>
                        </div>
                        <div class="col-lg-6 col-xl-6">
                            <label class="tx-11 fw-bolder mb-0 text-uppercase">Email:</label>
                            <label class="text-muted">@Model.user.Email</label>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-lg-6 col-xl-6">
                            <label class="tx-11 fw-bolder mb-0 text-uppercase">Date of Birth:</label>
                            <label class="text-muted">@Model.user.DateOfBirth.ToString("dd/MM/yyyy")</label>
                        </div>
                        <div class="col-lg-6 col-xl-6">
                            <label class="tx-11 fw-bolder mb-0 text-uppercase">Bank:</label>
                            <label class="text-muted">@Model.account.Bank.Name</label>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-lg-6 col-xl-6">
                            <label class="tx-11 fw-bolder mb-0 text-uppercase">Gender:</label>
                            <label class="text-muted">@Model.user.Gender</label>
                        </div>
                        <div class="col-lg-6 col-xl-6">
                            <label class="tx-11 fw-bolder mb-0 text-uppercase">Account Name:</label>
                            <label class="text-muted">@Model.account.AccountName</label>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-lg-6 col-xl-6">
                            <label class="tx-11 fw-bolder mb-0 text-uppercase">Role:</label>
                            <label class="text-muted">@Model.user.Role</label>
                        </div>
                        <div class="col-lg-6 col-xl-6">
                            <label class="tx-11 fw-bolder mb-0 text-uppercase">Account Number:</label>
                            <label class="text-muted">@Model.account.AccountNumber</label>
                        </div>
                    </div>
                </div>*@
                @*<div class="d-flex justify-content-between align-items-baseline">
            <h6 class="card-title mb-0"></h6>
            <div class="row">
                <div class="col-md-6">
                    <label class="tx-11 fw-bolder mb-0 text-uppercase">Full Name:</label>
                    <label class="text-muted">@Model.user.LastName @Model.user.FirstName @Model.user.MiddleName</label>
                </div>
                <div class="col-md-6">
                    <label class="tx-11 fw-bolder mb-0 text-uppercase">Email:</label>
                    <label class="text-muted">@Model.user.Email</label>
                </div>
            </div>
            <div class="row mt-3">
                <div class="col-md-6">
                    <label class="tx-11 fw-bolder mb-0 text-uppercase">Date of Birth:</label>
                    <label class="text-muted">@Model.user.DateOfBirth.ToString("dd/MM/yyyy")</label>
                </div>
                <div class="col-md-6">
                    <label class="tx-11 fw-bolder mb-0 text-uppercase">Bank:</label>
                    <label class="text-muted">@Model.account.Bank.Name</label>
                </div>
            </div>
            <div class="row mt-3">
                <div class="col-md-6">
                    <label class="tx-11 fw-bolder mb-0 text-uppercase">Gender:</label>
                    <label class="text-muted">@Model.user.Gender</label>
                </div>
                <div class="col-md-6">
                    <label class="tx-11 fw-bolder mb-0 text-uppercase">Account Name:</label>
                    <label class="text-muted">@Model.account.AccountName</label>
                </div>
            </div>
            <div class="row mt-3">
                <div class="col-md-6">
                    <label class="tx-11 fw-bolder mb-0 text-uppercase">Role:</label>
                    <label class="text-muted">@Model.user.Role</label>
                </div>
                <div class="col-md-6">
                    <label class="tx-11 fw-bolder mb-0 text-uppercase">Account Number:</label>
                    <label class="text-muted">@Model.account.AccountNumber</label>
                </div>
            </div>
        </div>*@
                @*<p class="text-muted">Sales are activities related to selling or the number of goods or services sold in a given time period.</p>
        <div id="monthlySalesChart"></div>*@
            </div>
        </div>
    </div>
    <div class="col-lg-4 col-xl-3 grid-margin stretch-card">
        <div class="row">
            <div class="col-md-12 grid-margin">
                <div class="card rounded">
                    <div class="card-body">
                        <h6 class="card-title">Referral Usage</h6>
                        <div class="table-responsive">
                            <table class="table">
                                <thead>
                                    @*<tr>
                                        <th>State</th>
                                    </tr>*@
                                </thead>
                                <tbody>
                                    <tr>
                                        <td>@Html.DisplayFor(modelItem =>Model.totalReferralUsage)</td>
                                    </tr>
                                    @*@if (Model != null && Model.StateRecord.Count > 0)
        {
            @foreach (var item in Model.StateRecord)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.State) - @Html.DisplayFor(modelItem => item.Country)</td>
                </tr>
            }}*@
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
            @*<div class="col-md-12 grid-margin">
                <div class="card rounded">
                    <div class="card-body">
                        <h6 class="card-title">Selected Programs</h6>
                        <div class="table-responsive">
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th>Category</th>
                                        <th>Program</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @if (Model != null && Model.Programs.Count > 0)
                                    {
                                        @foreach (var item in Model.Programs)
                                        {
                                            <tr>
                                                <td>@Html.DisplayFor(modelItem => item.ProgramCategory)</td>
                                                <td>@Html.DisplayFor(modelItem => item.Program)</td>
                                            </tr>
                                        }}
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>*@
        </div>
        @*<div class="card">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-baseline">
                    <h6 class="card-title mb-0">Cloud storage</h6>
                    <div class="dropdown mb-2">
                        <a type="button" id="dropdownMenuButton5" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            <i class="icon-lg text-muted pb-3px" data-feather="more-horizontal"></i>
                        </a>
                        <div class="dropdown-menu" aria-labelledby="dropdownMenuButton5">
                            <a class="dropdown-item d-flex align-items-center" href="javascript:;"><i data-feather="eye" class="icon-sm me-2"></i> <span class="">View</span></a>
                            <a class="dropdown-item d-flex align-items-center" href="javascript:;"><i data-feather="edit-2" class="icon-sm me-2"></i> <span class="">Edit</span></a>
                            <a class="dropdown-item d-flex align-items-center" href="javascript:;"><i data-feather="trash" class="icon-sm me-2"></i> <span class="">Delete</span></a>
                            <a class="dropdown-item d-flex align-items-center" href="javascript:;"><i data-feather="printer" class="icon-sm me-2"></i> <span class="">Print</span></a>
                            <a class="dropdown-item d-flex align-items-center" href="javascript:;"><i data-feather="download" class="icon-sm me-2"></i> <span class="">Download</span></a>
                        </div>
                    </div>
                </div>
                <div id="storageChart"></div>
                <div class="row mb-3">
                    <div class="col-6 d-flex justify-content-end">
                        <div>
                            <label class="d-flex align-items-center justify-content-end tx-10 text-uppercase fw-bolder">Total storage <span class="p-1 ms-1 rounded-circle bg-secondary"></span></label>
                            <h5 class="fw-bolder mb-0 text-end">8TB</h5>
                        </div>
                    </div>
                    <div class="col-6">
                        <div>
                            <label class="d-flex align-items-center tx-10 text-uppercase fw-bolder"><span class="p-1 me-1 rounded-circle bg-primary"></span> Used storage</label>
                            <h5 class="fw-bolder mb-0">~5TB</h5>
                        </div>
                    </div>
                </div>
                <div class="d-grid">
                    <button class="btn btn-primary">Upgrade storage</button>
                </div>
            </div>
        </div>*@
    </div>
</div> <!-- row -->
<!--<div class="row profile-body">
    <!-- left wrapper start -->
    <!--<div class="d-none d-md-block col-md-9 col-xl-9 left-wrapper">
        <div class="card rounded">
            <div class="card-body">
                <div class="d-flex align-items-center justify-content-between mb-2">
                    <h6 class="card-title mb-0"></h6>
                </div>
                <div class="row mt-3">
                    <div class="col-md-6">
                        <label class="tx-11 fw-bolder mb-0 text-uppercase">Full Name:</label>
                        <label class="text-muted">@Model.user.LastName @Model.user.FirstName @Model.user.MiddleName</label>
                    </div>
                    <div class="col-md-6">
                        <label class="tx-11 fw-bolder mb-0 text-uppercase">Email:</label>
                        <label class="text-muted">@Model.user.Email</label>
                    </div>
                </div>
                <div class="row mt-3">
                    <div class="col-md-6">
                        <label class="tx-11 fw-bolder mb-0 text-uppercase">Date of Birth:</label>
                        <label class="text-muted">@Model.user.DateOfBirth.ToString("dd/MM/yyyy")</label>
                    </div>
                    <div class="col-md-6">
                        <label class="tx-11 fw-bolder mb-0 text-uppercase">Bank:</label>
                        <label class="text-muted">@Model.account.Bank.Name</label>
                    </div>
                </div>
                <div class="row mt-3">
                    <div class="col-md-6">
                        <label class="tx-11 fw-bolder mb-0 text-uppercase">Gender:</label>
                        <label class="text-muted">@Model.user.Gender</label>
                    </div>
                    <div class="col-md-6">
                        <label class="tx-11 fw-bolder mb-0 text-uppercase">Account Name:</label>
                        <label class="text-muted">@Model.account.AccountName</label>
                    </div>
                </div>
                <div class="row mt-3">
                    <div class="col-md-6">
                        <label class="tx-11 fw-bolder mb-0 text-uppercase">Role:</label>
                        <label class="text-muted">@Model.user.Role</label>
                    </div>
                    <div class="col-md-6">
                        <label class="tx-11 fw-bolder mb-0 text-uppercase">Account Number:</label>
                        <label class="text-muted">@Model.account.AccountNumber</label>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="d-none d-md-block col-md-3 col-xl-3">
        <div class="row">
            <div class="col-md-12 grid-margin">
                <div class="card rounded">
                    <div class="card-body">
                        <h6 class="card-title">Work Coverage</h6>
                        <div class="table-responsive">
                            <table class="table">
                                <thead>-->
                                    @*<tr>
                                            <th>State</th>
                                        </tr>*@
                                @*</thead>
                                <tbody>
                                    @if (Model != null && Model.StateRecord.Count > 0)
                                    {
                                        @foreach (var item in Model.StateRecord)
                                        {
                                            <tr>
                                                <td>@Html.DisplayFor(modelItem => item.State) - @Html.DisplayFor(modelItem => item.Country)</td>
                                            </tr>
}}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-md-12 grid-margin">
                <div class="card rounded">
                    <div class="card-body">
                        <h6 class="card-title">Selected Programs</h6>
                        <div class="table-responsive">
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th>Category</th>
                                        <th>Program</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @if (Model != null && Model.Programs.Count > 0)
                                    {
                                        @foreach (var item in Model.Programs)
                                        {
                                            <tr>
                                                <td>@Html.DisplayFor(modelItem => item.ProgramCategory)</td>
                                                <td>@Html.DisplayFor(modelItem => item.Program)</td>
                                            </tr>
}}
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>-->*@
    <!-- right wrapper end -->
<!--</div>-->

